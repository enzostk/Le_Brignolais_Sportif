<div class="container">	
	<div class="text-center">
		<h1>A vos agendas ! <br> Voici les rendez-vous brignolais à ne pas manquer</h1>
	</div>
	
	<div class="row row-cols-2 row-cols-sm-2 row-cols-md-2 g-2">
		<% @events.each do |event| %>
		<div class="col">
			<div class="card shadow-sm">
				<%= image_tag event.image_url, size:"700x680"%>
			</div>
			<%= link_to "Voir le détail →", event, class:"btn btn-primary" %>
		</div>
		<% end %>
	</div>
</div>
