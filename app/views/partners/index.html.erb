<div class="container">
	<div class="text-center">
		<h2>Merci aux fidèles partenaires du Brignolais Sportif ! <br> Sans eux le projet n'aurait jamais existé !</h2>
		<hr>
		<br>
	</div>
	<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
		<% @partners.each do |partner| %>
		<div class="col">
			<div class="card shadow-sm">
			  <%= image_tag partner.image_url, size: 200*100 %>
				<div class="card-body">
					<p class="card-text b">
						<%= partner.description %>
					</p>
					<div class="d-flex justify-content-between align-items-center">
						<div class="btn-group">
							<button type="button" class="btn btn-sm btn-outline-secondary">
								<%= link_to partner.name, partner.link_to %>
							</button>
						</div>
					</div>
				</div>
			</div>
		</div>
		<% end %>
	</div>
</div>
