<div class="container">
  <p id="notice"><%= notice %></p>
  <p>
    <strong>Titre :</strong>
    <%= @album.title %>
  </p>

  <p>
    <strong>Categorie :</strong>
    <%= @album.category %>
  </p>

  <p>
    <%= image_tag @album.image_url %>
  </p>

  <p>
    <strong>Voir l'album :</strong>
    <%= link_to "👉 Album photo", @album.link_to %>
  </p>

  <%= render partial: "comments/comments", locals: {commented_on: @album, parent: nil} %>
  <% if current_user && current_user.subscriber || current_user && current_user.admin %>
  <%= render partial: "comments/form", locals: {commented_on: @album, parent: nil} %>
  <% end %>
  <br>
  <%= link_to "Retour à l'accueil", albums_path, class:"btn btn-light"%>
</div>
