<div class="container">
	<div class="text-center">
		<h2>Albums photos 📷 <br> Le lien vers l'album se trouve sa photo !</h2>
		<hr>
		<br>
	</div>
	<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
		<% @albums.each do |album| %>
		<div class="col">
			<div class="card shadow-sm">
				<a href="<%= album.link_to %>"><%= image_tag album.image_url, size: 200*100 %></a>
				<div class="card-body">
					<p class="card-text b">
						<%= album.title %>
					</p>
					<div class="d-flex justify-content-between align-items-center">
						<div class="btn-group">
							<button type="button" class="btn btn-sm btn-outline-secondary">
								<a href="<%= album.link_to %>">👉 Album photo</a>
							</button>
						</div>
					</div>
				</div>
			</div>
		</div>
		<% end %>
	</div>
</div>
