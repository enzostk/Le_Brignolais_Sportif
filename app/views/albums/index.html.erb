<div class="container">
	<div class="text-center">
		<h2>📷 Albums photos 📷 </h2>
		<p>(Cliquez sur l'image pour réagir aux albums!) </p>
		<hr>
	</div>
	<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
		<% @albums.reverse.each do |album| %>
		<div class="col">
			<div class="card shadow-sm">
				<%= link_to (image_tag album.image_url), album %>
				<div class="card-body">
					<p class="card-text b">
						<%= album.title %>
					</p>
					<div class="d-flex justify-content-between align-items-center">
						<div class="btn-group">
							<button type="button" class="btn btn-sm btn-outline-secondary">
							<%= link_to "👉 Album photo", album.link_to %> 
							</button>
						</div>
					</div>
				</div>
			</div>
		</div>
		<% end %>
	</div>
</div>
