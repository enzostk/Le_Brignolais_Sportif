<div class="container">
  <p id="notice"><%= notice %></p>

  <h3 class="text-center">
    <strong><%= @article.title %></strong>  
  </h3>

  <p>
    <strong><u>Categorie :</u></strong>
    <%= @article.category %>
  </p>

  <p>
    <%= image_tag @article.image_url %>
  </p>
  <div class="card-footer bg-secondary border-dark">
  <br>
  <p>
  <strong>&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp<%= @article.content %></strong><br/><br/>
  </p>
  </div>
  
  <%= render partial: "comments/comments", locals: {commented_on: @article, parent: nil} %>
  <% if current_user && current_user.subscriber || current_user && current_user.admin %>
    <%= render partial: "comments/form", locals: {commented_on: @article, parent: nil} %>
  <% end %>
  <br>
  <%= link_to "Retour à l'accueil", articles_path, class:"btn btn-light" %>
</div>
